List folders contents by modification time
---

```bash
#newer -> older
ls -lat /path/to/folder/ # l: show metadata, a: show hide files t: sort by size  

#older -> newer
ls -lart /path/to/folder/ # l: show metadata, a: show hide files t: sort by size  r: reverse order
```
